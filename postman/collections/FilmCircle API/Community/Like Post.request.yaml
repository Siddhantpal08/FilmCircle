$kind: http-request
name: Like Post
method: POST
url: '{{baseUrl}}/api/community/posts/{{postId}}/like'
order: 5000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200 OK", () => pm.response.to.have.status(200));
      pm.test("Response confirms like action", () => {
          const json = pm.response.json();
          pm.expect(json).to.have.property("likes");
          pm.expect(json.likes).to.be.an("array");
      });
      pm.test("User ID is in likes array", () => {
          const json = pm.response.json();
          pm.expect(json.likes).to.include(pm.collectionVariables.get("userId"));
      });
